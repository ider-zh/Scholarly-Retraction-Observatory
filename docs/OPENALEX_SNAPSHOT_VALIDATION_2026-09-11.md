# OpenAlex 首次本地分析交付记录

## 来源与接受政策

- AWS 来源：`s3://openalex/data/parquet/`；清单发布日为 **2026-06-26**，不是下载日。
- 清单 SHA-256：`10c1785b4a6f9757823fc6dd005a052716e2c510ea751659cab4bcfba4c35e2a`。
- 2026-09-11 按项目所有者明确批准的 `retrospective-v1` 接受本地快照。当前 AWS 清单与本地清单一致；下载前清单和传输时间日志仍标记为缺失，不补造证据。
- 5,202 个文件、21 类实体，共 649,096,577 条记录、783,555,949,110 字节；清单文件大小、Parquet footer 行数和实体内 schema 核对通过，没有额外文件进入分析。
- 所有实体的 ID 缺失、重复检查通过；Works ID 格式检查通过。这不是全部数据页的逐字节完整性证明。
- RW 固定提交：`6ddb2c80121ab22e5db8d3db0dd0fb4b9d348dfa`，来源日期 2026-09-10；CSV SHA-256 为 `833a08c2a97513e9f7185d0f0c23a1c3751e44bcb061abe5dd1fa127552a75ee`。

## 处理与结果

完整处理 2,446 个 Works 分片、510,372,821 条 Work 记录。主扫描使用 8 个 worker × 6 个线程，每 worker 内存上限 32 GB；维度分母扫描使用 4 × 4 线程，每 worker 16 GB。针对机械盘随机读取瓶颈，暂停分析后顺序预热剩余投影列的 OS 缓存，再恢复扫描；没有复制另一份快照。线程和内存配置是上限，不代表持续利用率。

| 总体 | 数量 | 含义 |
| --- | ---: | --- |
| A0 | 114,538 | core 撤稿标记 Work 记录 |
| A1 | 50,331 | core article 角色筛选后候选 |
| B | 66,700 | RW 去重 Retraction 原论文 |
| C | 46,283 | 高置信匹配的不同 OA Work，含不同类型和语料范围 |
| D | 216,416,450 | core article 同口径发表候选 |

这些总体重叠且范围不同，不能相加，也不能直接用概览中的 C 除以 D。比例图使用单独筛选的 C_D。OA 撤稿标记缺失为 1,869,938 条，保留为 unknown，不当作 false。

## 交付与边界

- 发布 ID：`oa-2026-06-26-b9575258ff4f`。本地入口：`#/snapshot/overview`，共九个章节、65 个 ready 预计算切片。
- 原 v2 报告保留；新增十个 v3 聚合 JSON。全站公开数据合计 **1,740,847 字节**，保留原有 36 条样本，未发布原始记录、匹配明细或引用边。
- 描述统计和选定的发表年份、Field、国家、期刊分母已完成；分母分区核对、分子包含关系、匹配核对和分数权重守恒通过。
- C2–C4 入边引用分析未计算；原因家族映射和 publisher 汇总未交付。原始原因标签分析不等于已审定的不端行为分类。界面与 manifest 明确区分 unavailable 和零。
- 测试：37 项 Python 测试、7 项 JavaScript schema 测试通过；生产构建和公共数据守卫通过。构建保留原应用大于 500 kB 的 bundle 提示。
- 真实聚合资产通过九章节浏览器检查、无效切片重置和 CSV 导出检查；390/768/1440 px 布局无页面级横向溢出。九章节 axe WCAG A/AA 自动检查无违规；自动检查不替代人工可访问性审查。

本地可复用运行目录：

```text
/mnt/hg02/openalex-snapshot/analysis/runs/0a8226b31ea961c53cfbf4525b0cb89ffe187d26a3225d651341b03ceedb80a1
```

来源审计位于同盘 `analysis/validation/validated-source.json` 指向的不可变审计目录。复现命令和限制见 [运行手册](OPENALEX_SNAPSHOT_RUNBOOK.md)。本次仅安装本地发布资产，没有远程部署。
