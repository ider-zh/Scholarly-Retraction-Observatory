# PPT 第 10、12 页：趋势关键值

本地调整，不提交、推送或部署。沿用现有 academic-paper 模板。

- 图内直接标注各学科完整发表年份的峰值及年份；并列展示最近一个共同可比较的完整发表年份（当前为 2025）的数值。
- 比例页同时显示该年的 n/N。实心点为峰值，空心点为比较年份；完整发表年份不等于随访结束。
- 三面板继续共用零起点纵轴。保留缺失断线、小基数叉号、未完整年度虚线、证据表、导出及深链接，不用独立缩放夸大差异。
- 平板和手机纵向排列，避免三个窄面板挤压标注。桌面保持并排。
- 数字从既有图表行生成；未修改公开聚合、分母、来源和流水线。构建更新静态 PPT 与首页 PPT 构建时间。

实际验证：`npm run build:presentation`；`node --test tests/presentation*.test.mjs`（19 项通过）；现有 `scripts/check-presentation-browser.mjs` 的全部页面三尺寸、axe、键盘、证据弹窗与导出检查通过；公开数据检查通过。

截图：修改前 `/tmp/sro-ppt-values/before/{10,12}.png`；修改后 `/tmp/sro-ppt-values/labels/`，含 1440×900、768×1024、390×844 两页及各面板截图。桌面压缩空白后的确认图为 `desktop-{10,12}-confirm.png`。浏览器全套证据在 `/tmp/sro-ppt-values/after/`。

限制：移动端和平板需纵向滚动；这里只优化既有三个学科，不增加指标或推断近期风险下降。
